---
- name: Include OS tasks
  include_tasks: "{{ ansible_os_family }}.yml"

- name: Copy scripts to /usr/local/bin
  copy:
    src: "{{ item }}"
    dest: /usr/local/bin/
    mode: 0755
  with_items:
    - nmap-scan.sh
    - nmap-xml-to-json.sh
